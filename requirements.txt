numpy>=1.26.0
git+https://github.com/Kaggle/kaggle-environments.git@master

# Backend
fastapi>=0.110
uvicorn[standard]>=0.28
httpx>=0.27

# Schemas
pydantic>=2.0

# Tournament
trueskill>=0.4.5
pyyaml>=6.0
psutil>=5.9

# RL agent (kashiwaba-rl). CPU-only wheel — Docker image stays under 1.5GB.
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.0

# Dev / test
pytest>=8.0
pytest-asyncio>=0.23
